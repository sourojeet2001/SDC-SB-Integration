/**
 * @file
 * SDCTest behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.sdctest = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
